#+TITLE: Customization & appearance
#+OPTIONS: toc:t
#+STARTUP: inlineimages

[[file:Index.org][↑ Back to the documentation index]]

This config is meant to be made yours. This page collects the taste-based
choices — colors, fonts, icons, languages — and points to where every setting
lives. (Settings that shape the *planning view* itself, like the agenda
sections and the mode line, are covered in [[file:Agenda.org][The Agenda]].)

* Where settings live
There are two layers, modelled on an editor's *User* vs *Workspace* settings:

- =config.org= — *system* settings, the same in every vault. Its /Settings/
  section (right after the bootstrap) is the single place to adjust defaults:
  there's a block per feature (Appearance, Fonts, Availability, Conflicts, Mode
  line, Task count, Material icons, File Tree, …). Everything about how things
  /look and behave/ belongs here.
- =workspace.org= — *vault* settings, belonging to one Org folder: your category
  icons, your file sets and their order, your context tags and situations, and
  whether that vault syncs. It lives beside your Org files, loads *after*
  =config.org=, and overrides it. Everything about how /this folder's content is
  organised/ belongs here. Keeping it in the vault means it travels with your
  notes, stays out of the public repo, and never conflicts when you pull
  upstream changes.

Switching vaults resets the vault settings and loads the new vault's, so a
category icon or situation defined in one does not follow you into another. See
[[file:Vaults.org][Vaults]].

Beside =workspace.org= sits =.ps/state.el=, which Emacs writes itself: it
remembers what the interface toggles (the active file set, whether it also
restricts the agenda) per vault. There is no reason to edit it by hand.

Open them from *Productivity → Config*: =C-c p C= opens =config.org= (=F10=
too), =C-c p W= opens =workspace.org=; the lower-case =C-c p R= / =C-c p w=
reload them. Copy =samples/realistic/workspace.org= as a starting point.

Every setting this config adds is a =ps/...= variable, so you can also browse
and change them with =M-x customize-apropos= → =^ps/= (or *Config → Customize
ps/ Settings* in the menu).

* Color theme
Switching the whole look is a one-line change. In =config.org=, /Settings →
Appearance/, set =ps/color-theme=:

#+begin_src elisp
(defvar ps/color-theme 'solarized-light  ; <- change this
  "Color theme loaded by `Editor & UI / Color theme'.")
#+end_src

External-package themes are *installed automatically* on first load.
Recommended values:

| Kind        | Source         | Examples                                                                            |
|-------------+----------------+-------------------------------------------------------------------------------------|
| Solarized   | package        | =solarized-light= (default), =solarized-dark=                                       |
| modus       | built-in (28+) | =modus-operandi=, =modus-vivendi=                                                   |
| ef-themes   | package        | =ef-day=, =ef-elea-dark=, =ef-winter=, =ef-autumn=                                  |
| standard    | package        | =standard-light=, =standard-dark=                                                   |
| doric       | package        | =doric-light=, =doric-dark=                                                         |
| doom        | package        | =doom-one=, =doom-one-light=, =doom-nord=, =doom-dracula=, =doom-gruvbox=           |
| Catppuccin  | package        | =batppuccin-latte=, =batppuccin-mocha=, =batppuccin-macchiato=, =batppuccin-frappe= |
| Tokyo Night | package        | =tokyo-night=, =tokyo-night-storm=, =tokyo-night-moon=, =tokyo-night-day=           |
| Gruvbox     | package        | =gruvbox-dark-medium=, =gruvbox-light-medium=                                       |
| Nord        | package        | =nord=                                                                              |

The config's own visual tweaks (faded DONE tasks, timestamp pills, metadata, the
SCHEDULED/DEADLINE icons) adapt to the chosen theme automatically, on light and
dark backgrounds alike.

** Audition a theme live
=M-x ps/preview-theme= (or =C-c p T=) loads any theme instantly so you can try
before you commit. Set =ps/color-theme= to keep it.

** Gallery
The default theme, *Solarized Light*:

#+CAPTION: Solarized Light (default)
[[file:../screenshots/solarized-light.png]]

A few others — light:

#+CAPTION: Modus Operandi (light, accessible)
[[file:../screenshots/modus-operandi.png]]

#+CAPTION: ef-day (light, warm)
[[file:../screenshots/ef-day.png]]

#+CAPTION: Doom One Light
[[file:../screenshots/doom-one-light.png]]

#+CAPTION: Catppuccin Latte (light, pastel)
[[file:../screenshots/batppuccin-latte.png]]

…and dark:

#+CAPTION: Solarized Dark
[[file:../screenshots/solarized-dark.png]]

#+CAPTION: Modus Vivendi (dark, accessible)
[[file:../screenshots/modus-vivendi.png]]

#+CAPTION: Doom One (dark)
[[file:../screenshots/doom-one.png]]

#+CAPTION: Tokyo Night (dark)
[[file:../screenshots/tokyo-night.png]]

#+CAPTION: Catppuccin Mocha (dark, pastel)
[[file:../screenshots/batppuccin-mocha.png]]

#+CAPTION: Gruvbox Dark
[[file:../screenshots/gruvbox-dark-medium.png]]

* Fonts
Fonts are set in =config.org= → /Settings → Fonts/, by *role* rather than one
font at a time:

| Setting                | Used for                                            |
|------------------------+-----------------------------------------------------|
| =ps/font-mono=         | The editing font, and everything aligned in columns |
| =ps/font-prose=        | Running text (the =variable-pitch= face)            |
| =ps/font-ui=           | Interface chrome — currently the file tree          |
| =ps/font-size=         | Body text size, in points                           |
| =ps/font-prose-scale=  | Size of running text relative to the body text      |
| =ps/font-ui-scale=     | Size of interface text relative to the body text    |
| =ps/font-line-spacing= | Extra space between lines, per font family          |

Each role is a *list* of font families in preference order, and the first one
your machine actually has is the one used. A family you don't have installed is
simply skipped, so you can name a font before installing it — or share the
config across machines — without anything breaking. Note that this is a
/fallback chain/, not a menu: listing four fonts doesn't offer a choice between
them, it just means the first one. To switch between several, see
[[*Live with a font before choosing it][Live with a font before choosing it]].

This is the same split VS Code and Obsidian make (Obsidian calls them the
Interface Font, the Text Font and the Monospace Font): the thing you're editing
and the chrome around it don't have to be the same font.

=ps/font-mono= has to be a monospaced font. It's not only the editing font: it
also defines the character width everything else is measured in, so the agenda,
the schedule ruler, and the availability and conflict reports all line their
columns up against it.

=ps/font-ui= has no such constraint — the file tree is a list of short labels,
not columns — so a proportional font is both allowed and easier to read there.

Sizes elsewhere follow =ps/font-size= automatically — headings, the line-number
gutter, and the category icons all scale with it, so changing that one number
resizes the whole interface.

Emoji and symbols your font doesn't cover come from other fonts, which can make
their line slightly taller than the rest. =ps/font-rescale-alist= shrinks those
fonts so every line keeps the same height; if a character still sits taller, add
its font there with a smaller factor. Revisit it if you change =ps/font-mono=.

** Line spacing
Different fonts want different space between lines, so =ps/font-line-spacing=
is set per font family, and switching fonts brings the right spacing with it:

#+begin_src elisp
(setq ps/font-line-spacing '(("Menlo" . 0.2)))
#+end_src

A float is a fraction of the line height, an integer is pixels. Families not
listed get no extra spacing. Press =l= in the font preview to try a value on
the current font.

** Audition fonts live
=M-x ps/font-preview= (or =C-c p P=) renders the same sample in every candidate
font, one block each, so you compare them against real content instead of by
name: the heading ramp at its configured sizes, a line of prose, an agenda row,
the characters the schedule ruler draws with, and regular / bold / italic side
by side. That last row is worth a look — a font without a real bold or italic
gets a smeared or mechanically slanted imitation from the system, which is only
obvious next to a font that has all four.

- =m= / =p= / =u= — use the font at point as the monospaced / prose / interface font
- =s= — change the body text size, =l= — change this font's line spacing
- =S= — change the prose or interface size relative to the body text
- =c= — cycle a role through your favourites
- =r= — put everything back the way Settings has it
- =g= — redraw, =q= — quit

Pressing =u= with the file tree on screen restyles the tree as you press it, so
tree fonts can be compared in place.

Applying a font from the preview is an *audition*: it changes what you see
without changing any setting, and shows you the exact line to paste into
=config.org= to keep it. =M-x ps/font-try= does the same for one font without
opening the buffer.

Fonts you haven't installed are listed at the bottom rather than drawn, so the
candidate list (=ps/font-preview-candidates=) can name fonts to try later. Most
are free and installable in one command, e.g. =brew install --cask
font-jetbrains-mono=. Note that the name to use is the family name the font
declares, which isn't always the name of the download.

** A proportional font for plan files
Plan files are almost entirely prose, so they don't need a monospaced font the
way the agenda and the schedule ruler do. =M-x ps/font-prose-toggle= (or =t= in
the font preview) draws them in =ps/font-prose= instead — immediately, in the
files already open.

It's off by default, since it's a large change to files you read every day. Set
=ps/font-prose-in-org= to =t= to make it permanent.

Only the plan files the agenda scans are affected — =config.org=,
=workspace.org=, the journal/archive and every generated view (Agenda, Tasks,
Calendar, Schedule) stay monospaced, because those genuinely are laid out in
columns. Inside a plan file the parts that are still column-aligned stay on the
grid too: tables, code blocks, inline code, the property drawer and
indentation. That list is =ps/font-prose-fixed-pitch-faces= if you need to add
to it.

One thing it can't reach: the /body/ of a =#+begin_src= block is coloured by the
source language's own faces rather than by any Org face, so it follows the prose
font. If you keep much code in plan files, that's the case to look at before
turning this on.

** Live with a font before choosing it
A preview tells you which fonts to rule out. It doesn't tell you which one you
want to read all day — that takes actually reading all day in it, and it's easy
to mistake a font you're /used to/ for a font you prefer. So put the ones that
survived the preview in =ps/font-favourites= and switch between them in place:

#+begin_src elisp
(setq ps/font-favourites
      '((mono  . ("Monaco" "Menlo" "IBM Plex Mono" "iA Writer Mono S"))
        (prose . ("IBM Plex Serif" "Charter" "iA Writer Quattro S"))
        (ui    . ("IBM Plex Serif" "SF Pro Text" "Inter"))))
#+end_src

=C-c p M= steps the editing font to the next one, =C-c p U= the file tree's.
Each applies immediately — no preview buffer, no restart — and neither is
saved, so the settings stay the source of truth until you decide. Each switch
also brings that family's line spacing with it.

#+begin_quote
Under the hood: =lisp/ps-fonts.el=; settings in =config.org= → /Settings →
Fonts (ps-fonts.el)/.
#+end_quote

* Reading width
Plan files wrap edge-to-edge on a maximized window, which reads better centred
in a column with margins — similar to Obsidian. Text is capped at a maximum
width and centred; the margins shrink to zero once the window itself is narrower
than that width, so nothing is lost on a split screen or a small frame.

Margins only apply to actual plan/prose files — the same ones the agenda
scans — never to =config.org=, =workspace.org=, the journal/archive, or a
generated view (Agenda, Tasks, Calendar, Schedule/Timeline). They're also off
during the =F7= blank-line-recovery review, since a diff reads better at full
pane width.

Change the maximum width, in characters, in =config.org= → /Settings →
Reading width/ (=ps/prose-width-columns=); set it to =0= to turn margins off
entirely.

* Selected text
Selecting text tints it rather than repainting it: the theme's own selection
colour is washed toward the page background, so headings, links and TODO pills
keep their own colours under a selection instead of turning inside out.

A selection also stays visible when you move to another window — the file tree,
the assistant's panel, a second file — drawn in a paler shade to show it is not
the window you are working in, and dimmed the same way while Emacs itself is
behind another application. Stock Emacs draws the region only in the window you
are in, which makes a selection look like it was thrown away; it never was, and
going back would show it again.

TODO and priority pills keep their colours inside a selection too. If anything
else washes out when you select it, add its face to =ps/selection-pinned-faces=.

Both shades follow whichever theme you load, so there is nothing to re-tune
after switching. In =config.org= → /Settings → Selection/:
- =ps/selection-pale= and =ps/selection-inactive-pale= — how faint the selection
  is (and the unfocused one): 0.0 is the theme's own colour at full strength,
  1.0 is invisible;
- =ps/selection-neutral= — from 1.0 (plain grey, the default) down to 0.0 (the
  theme's own tint);
- =ps/selection-show-inactive= — =nil= for stock Emacs behaviour;
- =ps/selection-dim-unfocused= — =nil= to keep one strength whether or not Emacs
  is in front;
- =ps/selection-keep-foreground= — =t= to let the theme repaint the text inside
  a selection again.

One thing this cannot change: Emacs always keeps the cursor inside the window
you are looking at, so scrolling far past an active selection drags its end
along with the cursor. Ask the assistant about a selection before scrolling
away from it — it is told about the last selection you made, so it still knows
the lines even when the editor has let go of them.

* Line numbers
Line numbers show in file buffers and in the Tasks view. In Org buffers they are
drawn smaller than the text and in a muted colour, the current line's number
included, so they stay readable without competing with the prose; code buffers
keep the ordinary gutter. Adjust the size and the colour in =config.org= →
/Settings → Line numbers/ (=ps/line-numbers-scale=, =ps/line-numbers-face=); a
scale of =1= leaves them at full size, and a face of =nil= leaves the colours
alone.

The gutter keeps a minimum width of two columns, so text doesn't shift sideways
as you scroll between short and long line numbers. Change the minimum in
=config.org= → /Editor & UI → Line numbers/.

* Scroll bar
The native scroll bar is replaced by a thin pill at the right edge of each
window, coloured to match your theme. Like the overlay scroll bars in macOS or
VS Code, it appears while you scroll or when the mouse is over it, then fades
away. Click anywhere along that edge to jump there. It shows in every window
except the AI assistant's panel, which has no scroll position to show.
Trackpad and mouse-wheel scrolling is smooth and pixel-precise.

In =config.org= → /Settings → Scroll bars/ you can change how long it stays
(=ps/scrollbar-hide-delay=), how quickly it fades (=ps/scrollbar-fade-duration=,
=0= to hide it at once), its width (=ps/scrollbar-width=) and whether hovering
reveals it (=ps/scrollbar-show-on-hover=). Set =ps/scrollbar-enabled= to =nil=
there to get the native scroll bar back.

* Category icons
Icons in the agenda and file tree come from the *Material Symbols Outlined* font
(install it per [[file:Installation.org][Installation]]). They're assigned *declaratively*, mapping a
=<Category>.org= file's basename to a [[https://fonts.google.com/icons][Material Symbols name]] — e.g.
=("Blog" . "edit_square")=.

=config.org= ships *no* mappings, so every file shows the generic =File= icon
until you provide a map. Do that in your =workspace.org= (so your categories stay
private):

- per file: =ps/material-icons-category-map= (=<Category>.org= → icon name);
- per folder: =ps/material-icons-folder-map= — the icon drawn for the *folder
  itself*, e.g. =("Current" . "calendar_month")=. Unmapped folders get a plain
  folder icon;
- for a folder's *contents*: =ps/material-icons-folder-contents-map= — one icon
  for every =.org= file under that folder, instead of listing them all.

Folder keys match a folder's name at any depth, or its path relative to the Org
base — so ="older"= icons every folder of that name, while ="ML/older"= icons
just the one. Category icons work the same wherever the file sits.

The mappings are read when the tree is built, so a file you create later in the
session shows the generic file icon rather than its category one until you run
=M-x ps/reload-workspace-config=.

See =samples/realistic/workspace.org= for a complete example. To fine-tune
size/alignment, adjust =ps/material-icons-height-scale= and
=ps/file-tree-icon-ascent= in =config.org= → /Settings/.

Icon colour follows the color theme and changes with it, including while you
audition themes: most themes draw icons in their muted text colour, and
Solarized in its body-text colour. To pick your own, set
=ps/material-icons-color= in =config.org= → /Settings → Material icons/ to a
face (its colour is used) or to a fixed colour such as ="#5f6368"=.

* File tree layout
All of these live in =config.org= → /Settings → File Tree/.

| Setting                            | What it does                                            |
|------------------------------------+---------------------------------------------------------|
| =ps/file-tree-hide-root=           | don't name the Org directory at the top (default on)    |
| =ps/file-tree-top-level-spacing=   | gap above each top-level folder, in fractions of a line |
| =ps/file-tree-bold-top-level=      | draw top-level folders bold, as section headers         |
| =ps/file-tree-order=               | explicit folder order; see below                        |
| =ps/file-tree-show-toggle-button=  | collapse/expand-all button in the top-right corner      |
| =ps/file-tree-follow-current-file= | highlight the file you're editing                       |
| =ps/file-tree-root-mode=           | =single= (one tree) or =subdirs= (a root per folder)    |
| =ps/file-tree-ignored-files=       | names never shown in the tree                           |

** Ordering the folders
By default entries are alphabetical. =ps/file-tree-order= overrides that: names
listed before the =:rest= keyword are pinned to the top in that order, names
after it are pinned to the bottom, and everything unlisted fills the middle
alphabetically. A name matches an entry by its file name, its name without the
=.org= extension, or its path relative to the Org base — the same matching the
icon maps use. Put it in your =workspace.org=:

#+begin_src elisp
(setq ps/file-tree-order '(:rest "Current" "Vision"))
#+end_src

Folders always come before loose files, so "bottom" means the bottom of the
*folder* list: files sitting directly in your Org directory still come after
every folder, whatever this says.

** What the tree hides
Two different lists decide what you see. =ps/file-tree-ignored-files= is about
*display* — it hides =init.org=, =workspace.org=, =AGENTS.md=, =CLAUDE.md=,
dotfiles and build artifacts from the tree. What the *agenda* reads is a
separate question, answered by =ps/org-files-exclude-directories= and
=ps/org-files-exclude-files= (see [[file:Planning-setup.org][Planning setup]]). A file can be in one list
and not the other: your journal is scanned out of the agenda but still browsable
in the tree.

* Live markup preview while editing
Org markup — =*bold*=, =/italic/=, =_underline_=, =+strikethrough+=, =code=,
=~verbatim~=, link brackets, entities like =\alpha=, and sub/superscripts like
=x_1= — renders clean, with the markup symbols hidden, like a live preview.
Move point onto a marked-up bit of text and the raw syntax reappears so you
can edit it. It stays visible for as long as point is inside it — typing
included, so the markers never flicker away mid-word — and hides again once
you move out. This applies everywhere except the read-only documentation
viewer (these pages), which always keeps markers hidden since you're not
editing there.

While you type the leading stars of a new heading, they show exactly as typed
(=*=, =**=, =***=); the heading bullet appears once you type the space after
them.

* Where headline tags sit
Tags appear exactly where you typed them. Whatever spacing stands between the
headline text and the first =:= in the file is the spacing you see — one space,
or five, or a whole column of them:

#+begin_example
** TODO Write the proposal :work:writing:
#+end_example

Nothing re-spaces a headline behind your back. Org's own default is to pad every
tagged headline out to a fixed column, rewriting the line as you type on it, as
you set tags, and as you promote or demote it; that is switched off here, so
your files keep the spacing you gave them.

When a headline is too long for the window, the tags move to the next line
*together*, and the group is never split.

* Typo / spell checking
Org buffers get a quiet, multilingual typo checker. A word is underlined with a
subtle wavy line *only when none of your configured languages recognise it*, so
terms valid in any language you write in are never flagged (e.g. Spanish =calor=
or Cyrillic words inside English text stay clean). Code, links, file paths, and
=verbatim=/=~code~= markup are skipped. Press =M-$= on an underlined word to pick
a correction or add it to your personal dictionary. Only individual words are
checked — no grammar checking, no network/LLM use.

The accepted languages default to English, Russian, and Spanish. Edit
=ps/typo-languages= in =config.org= → /Settings → Typo / spell checking/ to match
the languages you write in. Typo checking runs only in editable Org files —
these documentation pages, which open read-only, are never spell-checked.

This uses [[https://github.com/minad/jinx][Jinx]], which needs the =enchant= library plus a dictionary per
language (see [[file:Installation.org][Installation]]). On macOS, English works out of the box via the
system speller; for stricter checking or for Russian, install Hunspell
dictionary files (=<locale>.aff= / =<locale>.dic=) into
=~/.config/enchant/hunspell/=. List what =enchant= can see with =enchant-2
-list-dicts=. Dictionaries are available from the [[https://github.com/LibreOffice/dictionaries][LibreOffice dictionaries]] repo.
If =enchant= isn't installed, the rest of the config still loads — typo checking
just stays off.

* Under the hood / Customize
- Reading-width margins: =lisp/ps-prose-width.el= (uses the =visual-fill-column=
  package), wired in =config.org= → /Org Mode → Appearance → Reading width
  margins/; settings in =config.org= → /Settings → Reading width/
  (=ps/prose-width-columns=).
- Theme machinery and live preview: =config.org= → /Editor & UI/
  (=ps/color-theme=, =ps/preview-theme=).
- Selected text: =lisp/ps-selection.el=, wired in =config.org= → /Editor & UI →
  Selection/; settings in =config.org= → /Settings → Selection/
  (=ps/selection-pale=, =ps/selection-inactive-pale=,
  =ps/selection-neutral=, =ps/selection-show-inactive=,
  =ps/selection-dim-unfocused=, =ps/selection-keep-foreground=,
  =ps/selection-pinned-faces=).
- Line-number gutter width: =config.org= → /Editor & UI → Line numbers/
  (=display-line-numbers-width=).
- Line-number size and colour in Org buffers: =lisp/ps-line-numbers.el=, wired in
  =config.org= → /Editor & UI → Line numbers/; settings in =config.org= →
  /Settings → Line numbers/ (=ps/line-numbers-scale=, =ps/line-numbers-face=).
- Scroll bar: =lisp/ps-scrollbar.el=; settings in =config.org= → /Settings →
  Scroll bars/ (a few more =ps/scrollbar-...= options are listed there, and its
  colour is the =ps/scrollbar-thumb= face). Smooth scrolling: =config.org= →
  /Editor & UI → Smooth scrolling (ultra-scroll)/.
- Live markup preview: the =org-appear= package plus =lisp/ps-appear.el= (which
  keeps the raw syntax up while you type in it), both wired in =config.org= →
  /Appearance → Show formatting markup only at point/. No settings.
- Literal heading stars while typing: =lisp/ps-heading-stars.el=, wired in
  =config.org= → /Appearance → Keep heading stars visible while typing them/.
- Material icons: =lisp/ps-material-icons.el= (+ =ps-agenda-icons.el=,
  =ps-file-tree-icons.el=); settings in =config.org= → /Material icons/ and
  /File Tree/.
- Typo checker: =lisp/ps-typo.el=; settings in =config.org= → /Typo / spell
  checking/.
- Headline tags kept whole when a line wraps: =lisp/ps-tags.el= (no settings);
  Org's own re-spacing is switched off in =config.org= → /Org Mode → Appearance/
  (=org-auto-align-tags=).
