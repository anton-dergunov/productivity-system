

TODO Use this to describe how to use Tasks view:

/ filter by tag (prefix the tag with - to exclude), \ add another tag filter
= filter by regexp, < filter by the category at point, _ filter by effort
| remove all filters
0 r … 6 r switch which TODO keyword is shown (the selector header we're restoring); r/g rebuild
n/p/arrows move, RET jump to the task, SPC/TAB peek, t change state, , priority, : set tags, q quit



